{{#> layout }}
  {{#> heading }}
    Sveiki, {{ firstName }}!
  {{/heading }}
  {{#> paragraph }}
    Sveikiname gavus naują rezervaciją paslaugai <i>„{{ service }}”</i>, kuri prasideda <i>{{ time }}</i>.
  {{/paragraph }}
{{/layout }}