{{#> layout }}
  {{#> heading }}
    Sveiki, {{ firstName }}!
  {{/heading }}
  {{#> paragraph }}
    Pranešame, kad rezervaciją paslaugai <i>„{{ service }}”</i>, kuri prasideda <i>{{ time }} buvo atšaukta</i>.
  {{/paragraph }}
{{/layout }}